import React, {useState } from 'react';

const ReadFast = (props) => {
  return (
        <button className="read-fast-button" onClick={props.isReadFastOn} >
      
        </button>
  );
};
export default ReadFast;

